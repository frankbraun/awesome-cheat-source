/* C cheat source */

/* include guard to prevent including the header file multiple times */
#ifndef C_H
#define C_H

/* declaring a hello function which receives a name argument and returns an int
   (the error code). */
int hello(const char *name);

#endif /* C_H */
